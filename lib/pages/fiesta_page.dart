// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class Fiesta extends StatelessWidget {
  const Fiesta({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('fiesta_page funciona'),
    );
  }
}